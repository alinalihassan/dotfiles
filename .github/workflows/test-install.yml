name: Test Dotbot on macOS

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - name: Run Dotbot in fake home
        run: |
          export HOME=$(mktemp -d)
          ./install -v
          ls -la $HOME
